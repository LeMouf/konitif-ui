/** Shared presentation identifiers for the footer's mutually exclusive controls. */
export type FooterControlId = 'docs' | 'layout' | 'shortcuts' | 'audio' | 'runtime' | 'language' | 'reading' | 'theme';
